Программа демонстрирует работу стека, элементами которого могут быть только целые числа. Стек автоматически увеличивается по мере надобности. Поддерживаются следующие команды:

create [размер]   Создает пустой стек указанного размера. Если стек уже существует, он будет перезаписан.
check   Позволяет проверить, пуст ли стек.
push [число]   Помещает число в стек.
pushAll [числа]   Помещает все последующие числа в стек.
show   Отображает текущее состояние стека.
depth [искомое число]   Возвращает число ячеек между верхом стека и искомым числом.
peek   Возвращает число, лежащее на верху стека, при этом не удаляя его из стека.
pop   Возвращает число, лежащее на верху стека и удаляет его из стека.
exit   Завершает работу программы.

